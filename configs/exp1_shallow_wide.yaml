# 实验一：浅而宽 (Shallow & Wide)
depth: 3
width: 128
epochs: 150
lr: 0.001
batch_size: 128
norm_type: bn
act_type: relu
pool_type: max

# Run command: python main.py --config configs/exp1_shallow_wide.yaml
